# verifyMail
A lightweight PHP API that verifies email addresses by checking MX records and performing SMTP validation. Returns detailed JSON results showing if an email is valid, deliverable, or undeliverable. Fast, simple, and easy to integrate into any project.
